apiVersion: v1
kind: Service
metadata:
  name: {{ include "shilp-server.fullname" . }}
  labels:
    {{- include "shilp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shilp-server.selectorLabels" . | nindent 4 }}
